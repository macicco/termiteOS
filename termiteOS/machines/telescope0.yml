engine0:
    type: engine
    host: localhost
    port: 5000
    nodes:
       - LX200:
            type: LX200
            host: localhost
            port: 5001
       - tracker:
            type: TLEtracker
            host: localhost
            port: 5002

